{
	"version": 2,
	"installCommand": "npm install",
  "buildCommand": "npm run build",
  "functions": {
    "api/todoServer.js": {
      "memory": 1024,
      "maxDuration": 25
    }
  },
	"rewrites": [
		{ "source": "/:path*", "destination": "/api/todoServer" }
	]
}
